const authService = {
  getSession: async () => {},
  login: async () => {},
  logout: async () => {},
  register: async () => {},
};

export type AuthService = typeof authService;
export default authService;
